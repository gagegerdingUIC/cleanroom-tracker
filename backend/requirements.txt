# Core backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database
sqlalchemy>=2.0.29
alembic>=1.13.1

# Authentication (phase 2 — install now, activate later)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# Spatial operations (die history query)
shapely>=2.0.4

# File processing
python-magic-bin>=0.4.14   # MIME type detection (bundles libmagic for Windows)

# PDF export (phase 2)
# weasyprint>=61.0

# Development / testing
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0          # ASGI test client for FastAPI
